services:
  postgres:
    image: 'postgres:latest'
    container_name: bossa-db
    environment:
      POSTGRES_DB: bossa
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: